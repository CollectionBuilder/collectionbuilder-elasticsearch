
require 'logger'

# Define global logger.
$logger = Logger.new(STDERR, level: Logger::WARN)

# See the *.rake files in rakelib/ for all task definitions.
# Note that this Rakefile must be present for the tasks in rakelib to be findable / executable.
